{
  "name": "node-cicd-sample",
  "version": "1.0.0",
  "description": "A sample Node.js project for CI/CD pipeline testing",
  "main": "mins.js",
  "scripts": {
    "start": "node mins.js",
    "test": "jest",
    "build": "echo 'Building the project...'",
    "predeploy": "node -e 'const add = require(\"./add\"); if (add(1, 2) !== 3) throw new Error(\"Test failed\");'"
  },
  "author": "Your Name",
  "license": "ISC",
  "devDependencies": {
    "jest": "^27.0.0"
  }
}
